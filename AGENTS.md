# AGENTS.md

Shared project instructions for AI coding agents (Claude Code, Codex, etc.).

## Agent skills

### Issue tracker

Issues live in this repo's GitHub Issues (rcrusoe88-bot/yuxiaomo-ai-notes), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
